fix(phase-r3): 架构整合与去重（Subagent/Memory 统一、配置安全、企业版修正）

Phase R3 完成 5 项架构整合，消除双 DB/双 schema/双实现导致的数据不共享与维护负担：

R3-A 配置安全统一：
- evolution_strategies: 默认禁用 auto_apply_high，防止自动应用高风险策略
- config_mutator: 使用 safe_writer + 文件锁确保配置文件原子写入
- model/fallback: 添加 Fallback 链环路检测，防止配置循环导致死循环

R3-B LLM 抽象整合 + 文档整合：
- 合并 9 份审计报告为 docs/comprehensive-audit-report.md 单一权威文档
- 旧报告归档到 docs/archive/，避免多份漂移

R3-C Subagent 合并（新增 subagent_db.py）：
- 统一 subagent_delegation / subagent_lifecycle 的 DB 访问层
- 定义 25 列超集 schema，处理 NOT NULL 约束冲突
- 两套实现共享同一 DB，消除数据不共享问题

R3-D Memory 合并（新增 shared_db.py）：
- 统一 MemoryManager / ThreeLayerMemory 的 DB 路径
- 术语映射：episodic<->short_term, semantic<->long_term
- migrate_legacy_episodic_db 幂等迁移旧 episodic.db 数据

R3-E 企业版功能修正：
- 移除未实现的 RABBITMQ/ETCD FeatureFlag，标注为 PLANNED
- backend_defaults 改为 redis/sqlite
- sso.py SAML stub 显式抛 SSOError，防止安全风险

测试隔离修复：
- test_settings.py: 显式清理 MAOP_AUTH 等环境变量，防止 e2e 测试模块级 os.environ 污染导致 test_default_values 间歇性失败

全量回归测试：4141 passed, 4 skipped
