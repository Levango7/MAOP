# Grafana dashboard provisioning — auto-load the MAOP observability dashboard.
apiVersion: 1

providers:
  - name: MAOP
    orgId: 1
    folder: MAOP
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /etc/grafana/dashboards