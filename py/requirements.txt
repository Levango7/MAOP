# MAOP Python service layer — 本地开发快速安装指引（非权威锁文件）。
#
# ⚠️ 本文件是「开发指引子集」：刻意排除企业版驱动 etcd3（2019 停更 + CVE + Python≥3.13 装不上，
#    需要时 `pip install "maop[etcd]"` 显式安装）。请勿据此做 SBOM / 漏洞审计。
#
# ✅ 权威锁文件是 requirements.lock（pip freeze 生成）：CI 的 SBOM(cyclonedx) 与 pip-audit
#    均以它为唯一输入。本地 `make install` 用本文件仅为避开 etcd3/torch 等重依赖，便于快速起环境。
#
# 与 pyproject.toml [project.dependencies] 对齐。

fastapi==0.141.1
uvicorn[standard]==0.30.6
pydantic>=2.9.0,<3.0
pydantic-settings==2.5.2
pyyaml==6.0.2
httpx>=0.27.0,<1.0
python-dotenv>=1.2.2
mmh3==5.2.1
# 5.2 fix: cap upper bounds (see pyproject.toml [project.dependencies]).
numpy>=1.24.0,<3.0
cryptography>=44.0,<51
lxml>=4.9.0
alembic>=1.13.0
python-multipart>=0.0.9
aiosqlite>=0.20.0
sqlite-vec>=0.1.6
aiohttp>=3.9.0
websockets>=12.0,<15
PyJWT>=2.8.0
packaging>=24.0

# ── 企业版分布式后端（生产必需）──────────────────────────────────
# PostgreSQL 驱动（二选一，推荐 psycopg3）
psycopg[binary,pool]>=3.1
asyncpg>=0.29.0

# Redis 驱动
redis>=5.0.0

# RabbitMQ 驱动
pika>=1.3.0

# etcd 驱动（P0-2: 2019 停更 + CVE + Python≥3.13 装不上，移出企业版默认；
# 需要时 pip install "maop[etcd]" —— 不推荐生产使用）
# etcd3>=0.12.0

# Vault 密钥管理
hvac>=2.0.0
